namespace SalamanderBank
{
    public class Currency
    {
        // The currency code (e.g., USD, EUR, GBP)
        public string? CurrencyCode;

        // The exchange rate for the currency compared to a base currency (e.g., USD)
        public float ExchangeRate;
    }
}